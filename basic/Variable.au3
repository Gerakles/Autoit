Dim $var, $MY_VAR, $_variable

$message = "Three"

Dim $parameters[3]

$parameters[0] = 64
$parameters[1] = "Boby 0.02"
$parameters[2] = "just do it"

MsgBox($parameters[0], $parameters[1], $parameters[2])